http {
    error_log /home/box/var/log/nginx_info_logs info;

    server {
        listen 80;

        location {
            location ^~ /uploads/ {
                root /home/box/web;
            }

            location ~* \.\w+$ {
                root /home/box/web/public;
            }
        }
    }
}